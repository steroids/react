import * as React from 'react';

import Button from '../Button';

/**
 * Block button
 * @order 4
 * @col 4
 */
export default () => (
    <>
        <Button
            block
            label={__('Block')}
        />
    </>
);
