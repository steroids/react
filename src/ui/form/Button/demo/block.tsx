import * as React from 'react';

import Button from '../Button';

/**
 * Кнопка растянутая по ширине родительского блока.
 * @order 4
 * @col 4
 */

export default () => (
    <>
        <Button
            block
            label={__('Block')}
        />
    </>
);
