import * as React from 'react';
import DateTimeField from '../DateTimeField';

export default () => (
    <>
        <DateTimeField
            label='Icon'
            dateProps={{
                icon: true,
            }}
        />
    </>
);
